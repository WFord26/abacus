import { PlaceholderPage } from "../../../src/components/placeholder-page";

export default function ReceiptsPage() {
  return (
    <PlaceholderPage
      description="Receipt upload and linking will land on top of this scaffold after the documents service routes are wired through the gateway."
      eyebrow="Documents"
      title="Receipts"
    />
  );
}
