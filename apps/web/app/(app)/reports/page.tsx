import { PlaceholderPage } from "../../../src/components/placeholder-page";

export default function ReportsPage() {
  return (
    <PlaceholderPage
      description="Reporting views will connect to pre-aggregated service snapshots through the same typed API client introduced in this phase."
      eyebrow="Insights"
      title="Reports"
    />
  );
}
