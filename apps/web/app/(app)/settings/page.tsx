import { PlaceholderPage } from "../../../src/components/placeholder-page";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      description="Organization settings, membership management, and account preferences can now land inside the authenticated shell without new layout work."
      eyebrow="Workspace"
      title="Settings"
    />
  );
}
